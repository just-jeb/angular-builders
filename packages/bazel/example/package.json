{
  "name": "bazel-example",
  "version": "13.0.0",
  "scripts": {
    "build": "ng build && ng run example:timestamp"
  },
  "private": true,
  "devDependencies": {
    "@angular-builders/bazel": "latest"
  }
}
